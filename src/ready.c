#include "./includes/ready.h"

struct ready {
    // priority queue
};

void ready_init();
void ready_destroy();
void ready_push();
void ready_pop();
void ready_top();
void ready_empty();
void ready_size();
